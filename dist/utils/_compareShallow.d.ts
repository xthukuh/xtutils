/**
 * Compare multiple values with shallow matching
 * - compareTarget	= `args[0]`
 * - compareWith	= `args[1*]`
 *
 * @param a  Compare value 1
 * @returns `boolean` is match
 */
export declare const _compareShallow: (...args: any[]) => boolean;
